.DS_Store

# Claude Code: the commands are project conventions worth sharing, but nothing
# else under .claude is. release.env holds the Discord webhook URL and this
# repository is public, so it must never be committed.
.claude/*
!.claude/commands/
*.env

node_modules/
dist/
